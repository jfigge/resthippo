# ── Build outputs ─────────────────────────────────────────────────────────────
build/
dist/
data/

# ── Node / npm ────────────────────────────────────────────────────────────────
src/node_modules/
src/npm-debug.log*
.npm

# ── macOS ─────────────────────────────────────────────────────────────────────
.DS_Store
.AppleDouble
.LSOverride

# ── Editor / IDE ──────────────────────────────────────────────────────────────
*.swp
.vscode/

# ── Electron builder cache ────────────────────────────────────────────────────
.electron-builder-cache/

# ── Mock server ───────────────────────────────────────────────────────────────
mock/mock-server
mock/.mock.pid
mock/.mock.log

# ── Local dev environment (shared by make debug / launch / mock-up) ───────────
# Tracked template lives in dev.env.example; the real file stays local.
dev.env

# ── Data and local test files ─────────────────────────────────────────────────
data/collections.json
features/
CLAUDE.md
